[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# OpenClaw doesn't have /health endpoint, disable HTTP healthcheck
restartPolicyType = "always"
restartPolicyMaxRetries = 3

# Volume mount for persistent state
[[mounts]]
path = "/data"
